"use strict";
/**
 * Algory
 *
 * Copyright (c) Anurag Muthyam
 * https://github.com/aryaghan-mutum
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.selectionSortImper = void 0;
const Swap_1 = require("./Swap");
const curry = require('curry');
/**
 * Selection Sort
 *
 */
exports.selectionSortImper = curry((arr) => {
    debugger;
    // if the array is null or undefined then return null
    if (arr === undefined || arr === null)
        return null;
    // One by one move boundary of unsorted subarray
    for (let i = 0; i < arr.length; i++) {
        // Find the minimum element in unsorted array
        let min = i;
        for (let j = i + 1; j < arr.length; j++) {
            // if the elem in first index is greater than elem in second index the swap
            if (arr[min] > arr[j]) {
                min = j;
            }
        }
        if (min !== i) {
            Swap_1.swap(arr, i, min);
        }
    }
    return arr;
});
//# sourceMappingURL=SelectionSort.js.map